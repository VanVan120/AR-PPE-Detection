# Phase 3 — Activity / step recognition (Assembly101 temporal action segmentation)
# Trains & evaluates on the precomputed 2048-D TSM features (no raw-video CNN
# training), so a single consumer GPU is enough.
#
# The metrics + loader logic (Milestone 1) need only numpy (+ pandas for actions.csv).
# lmdb is needed to read the real feature stores; torch for the model (Milestone 2).

numpy>=1.26
pandas>=2.0           # actions.csv (class map)
lmdb>=1.4             # per-view TSM feature stores (runtime only)
torch>=2.0            # temporal model (C2F-TCN) — Milestone 2
tqdm>=4.66
